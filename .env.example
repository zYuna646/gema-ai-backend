# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_DATABASE=gema_ai_db

# Application Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration (if needed)
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRATION=1d